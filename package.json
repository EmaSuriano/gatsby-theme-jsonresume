{
  "name": "gatsby-starter-theme-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "gatsby-theme-jsonresume",
    "gatsby-starter-jsonresume"
  ],
  "scripts": {
    "start": "yarn workspace gatsby-starter-jsonresume start",
    "build": "yarn workspace gatsby-starter-jsonresume build",
    "test": "yarn workspace gatsby-starter-jsonresume test",
    "release": "yarn workspace gatsby-theme-jsonresume release"
  },
  "resolutions": {
    "html-to-text": "7"
  }
}
