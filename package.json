{
  "name": "gatsby-starter-theme-workspace",
  "private": true,
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn workspace example build && yarn workspace example serve",
    "test": "yarn workspace example build && yarn workspace example test"
  },
  "workspaces": [
    "gatsby-theme-jsonresume",
    "example"
  ]
}
